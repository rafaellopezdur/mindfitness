# Normaliza los finales de línea: LF en el repositorio, nativo en el equipo.
# Evita diffs falsos entre Windows, macOS y el servidor de CI.
* text=auto eol=lf

# Binarios: nunca convertir.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.svg text eol=lf
*.pdf binary
*.ico binary
